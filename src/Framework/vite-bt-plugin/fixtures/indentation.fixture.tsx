// @ts-nocheck
import { it, describe, expect } from '@framework';

describe('test group', () => {
  it('checks', () => {
    function y() {
      console.log('hello');
    }

    const x = 1;
    expect(1).toBe(1);
  });
});
